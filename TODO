all values (incl. nested) should be possibly ints

blocked - Unit tests in docker for testing rocksdb orm

Add an add endpoint for adding additional items to a key (requires a dictionary to be the current value or null at the key).
Add itest for add_to_row functionality, currently not tested, WIP
Add client functions for add_to_row like set, set_cached, and set_bulk

Figure out how to clear_all from rocksdb

In the client, do something with response from server
