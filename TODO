all values (incl. nested) should be possibly ints (is this not true now?)

blocked - Unit tests in docker for testing rocksdb orm

Add client functions for add_to_row like set, set_cached, and set_bulk. 
And clean up add functionality

Figure out how to clear_all from rocksdb

In the client, do something with response from server
